server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri:
            ${HOME}/IntelliJIDEAProjects/spring-boot-cloud-local-config-files
  application:
    name: spring-boot-cloud-config-server
